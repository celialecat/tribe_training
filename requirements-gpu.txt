# ==============================================================================
# GPU / deep-learning stack — install AFTER requirements.txt on CUDA hosts.
# ------------------------------------------------------------------------------
# Target: Ubuntu 24.04 · CUDA 12.1+ · Python 3.12 · NVIDIA GPU.
#
# Install torch from the CUDA-matched index first:
#
#   pip install torch torchvision torchaudio \
#       --index-url https://download.pytorch.org/whl/cu121
#
# Then install this file, then the official TRIBE v2 package (see below).
# ==============================================================================

torch>=2.3
torchvision>=0.18
torchaudio>=2.3

# Explainability + export extras (optional but installed on GPU nodes).
shap>=0.45
captum>=0.7
onnx>=1.16
onnxruntime-gpu>=1.18
matplotlib>=3.9

# ------------------------------------------------------------------------------
# Official Meta TRIBE v2 model. Installed from source per the upstream README:
#
#   git clone https://github.com/facebookresearch/tribev2
#   pip install -e "./tribev2[training]"
#
# Weights (facebook/tribev2, CC-BY-NC-4.0) are pulled from the HuggingFace Hub
# at first use into $TRIBE_CACHE_DIR. Do NOT vendor or mock this package.
# ------------------------------------------------------------------------------
huggingface-hub>=0.24
