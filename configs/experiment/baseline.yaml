# @package _global_
# Example packaged experiment. Select with:  ysp-train +experiment=baseline
# Demonstrates how to override any composed group from a single file.
run_name: baseline

training:
  epochs: 50
  lr: 3.0e-4

model:
  brain_encoder:
    n_layers: 4
    latent_dim: 512

data:
  batch_size: 16
