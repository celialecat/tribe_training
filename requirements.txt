# ==============================================================================
# Convenience requirements file for non-editable installs (CI, quick setups).
# ------------------------------------------------------------------------------
# The authoritative dependency list lives in pyproject.toml. This file exists
# so that `pip install -r requirements.txt` works in environments that do not
# install the project itself. It deliberately EXCLUDES the GPU/torch stack and
# the official `tribev2` package, which must be installed with CUDA-matched
# wheels — see requirements-gpu.txt and docs/installation.md.
# ==============================================================================

fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
hydra-core>=1.3
omegaconf>=2.3
sqlalchemy>=2.0
alembic>=1.13
yt-dlp>=2024.7.1
opencv-python-headless>=4.10
numpy>=1.26
pandas>=2.2
scipy>=1.13
scikit-learn>=1.5
tensorboard>=2.16
python-multipart>=0.0.9
httpx>=0.27
rich>=13.7
tqdm>=4.66
youtube-transcript-api>=0.6.2
